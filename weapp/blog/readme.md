# blog 小程序

- app.json 应用配置
  - appName navigationBar
  

- rpx  
  小程序独有的响应式单位
  管你是什么手机，都能适配
  宽度是 750rpx iphone 标准设备的单位

- 数据驱动界面
  - js Page({})
    data 绑定了数据
    wxml 模板 使用{{}}  来绑定数据
    block wx:for 循环指令来循环输出 默认每一项叫item 
    