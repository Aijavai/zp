# React Todos

- react + stylus + vite
- 父子组件通信
- 兄弟组件**通信**
  TodoInput TodoList TodoStats
  todo[] useState
  父组件负责持有数据 管理数据
  props 传递给子组件
  父组件还可以将修改数据的方法传递给子组件
  子组件不可以直接修改数据 只能通过父组件
  传递的方法来修改数据
  