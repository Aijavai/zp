# Array
- 数组是连续内存块，开箱即用的数据结构
- js 中，数组是一个特殊的对象

- c++ 或 java 中，数组是内存中连续的存储空间。
  但是JS 中数组复杂得多，
- 快数组 Fast Elements
  数组紧凑（索引连续，无空洞）且长度适中时，v8 为其分配连续内存。读写速度极快
- 慢数组 Dictionary Mode 
  数组非常稀疏时，或属性极其复杂时，v8 内部会将数组降级为hash map



## 数组的算法
- 双指针法  
  从后往前，优化空间复杂度


  左右指针
  快慢指针
- 滑动窗口
- 前缀和
