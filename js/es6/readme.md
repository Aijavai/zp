# es6 
2015 作为分水岭

## 解构 
- 一次性声明多个变量

## reset 运算符 ...

## 对象属性简写语法(Shorthand Property)
const sex = 'boy';
const obj = {
    name: 'songtaoliu',
    age: 18,
    sex,          // es6 Shorthand Property
}

## 模板字符串

## for of 循环 
  简单循环，语义更好
  相对于计数循环 

  for in 遍历数组（对象） 但性能不好，没有利用数组存储的连续性，每次入栈调用回调函数性能差。

## bigint