# 原型继承

## Prototype
对象有一个隐藏属性[[Prototype]] ，null , 对另一个对象的引用

设置[[Prototype]] 的方法，
- __proto__ ,object._proto_ = object1; // object1 设置为object 的原型。

- 原型链的两个限制：
- 引用不能形成闭环。
- _proto_ 的值只能是对象和null，其他的类型都会被忽略。
// 只能有一个[[Prototype]].